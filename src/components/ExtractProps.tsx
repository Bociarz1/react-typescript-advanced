import React from "react";
import GenericProps from "./GenericProps";

// if you can't export PROP TYPE from anyother component
function ExtractProps(props:React.ComponentProps<typeof GenericProps>) {
  return ( 
    <>
      
    </>
   );
}

export default ExtractProps;
